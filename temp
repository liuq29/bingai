直接安装setuptools_scm：
尝试直接安装setuptools_scm，以确保它在您的环境中可用：
pip3 install setuptools_scm

使用PEP 517构建：
如果问题仍然存在，您可以尝试使用PEP 517构建标准来安装包。这可以通过添加--use-pep517标志来实现：
pip3 install --use-pep517 "fschat[model_worker,webui]"

检查代理设置：
确保您的代理设置正确无误，并且代理服务器允许您访问所需的资源。
使用可信的源：
如果您收到关于不受信任或不安全主机的警告，您可以尝试添加--trusted-host参数来解决：
pip3 install --trusted-host mirrors.aliyuncs.com "fschat[model_worker,webui]"
